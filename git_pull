# git pull ignore config/database.yaml
# 변경 사항 무시 설정
git update-index --skip-worktree config/database.yaml

# 최신 변경 사항 가져오기
git pull origin main

# 변경 사항 무시 해제
git update-index --no-skip-worktree config/database.yaml
